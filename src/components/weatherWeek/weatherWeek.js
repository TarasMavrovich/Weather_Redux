import React from "react";

const WeatherForecast = ({ forecast }) => {
  if (!forecast) {
    return null;
  }

  return (
    <div>
      <h1>hi</h1>
    </div>
  );
};

export default WeatherForecast;
